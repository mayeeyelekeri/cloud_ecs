#!/bin/bash 

echo inside permissions, nothing to do
SECONDS=0

# just call the script which takes care of creating parameters 
### . ~/INFO/secrets/nw_params.sh  $1
 
echo "Elapsed Time to create permission stack: (using \$SECONDS): $SECONDS  seconds"
